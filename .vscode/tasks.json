{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": ["./gradlew build -x test"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "unit test",
            "type": "shell",
            "command": ["./gradlew test"]
        },
        {
            "label": "run App",
            "type": "shell",
            "command": ["./gradlew bootRun --console=plain"]
        }
    ]
}
